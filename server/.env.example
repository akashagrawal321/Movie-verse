# Server Environment Variables Configuration Template
PORT=5000
NODE_ENV=development

# MongoDB Connection String (Local or MongoDB Atlas Cluster)
MONGO_URI=mongodb://localhost:27017/movieverse

# JWT Secret Key for Token Signatures (Use a strong secret string in production)
JWT_SECRET=movieverse_super_secret_jwt_key_2026_interview_ready
